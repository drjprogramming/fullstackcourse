sequenceDiagram
  participant browser
  participant server

  browser->>server: POST
https://fullstack-exampleapp.herokuapp.com/new_note_spa
  activate server
  server-->>browser: 201 created status
  deactivate server

  Note right of browser: Form transmits data to server via JSON
  Note right of server: Server renders new note object and pushes it to notes array. No redirect
